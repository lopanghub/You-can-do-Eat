<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springbootstudy.app.mapper.MemberMapper">
	
	<insert id="insertMember" parameterType="MemberShip">
		insert into membership (name, profile, id, birthdate, pass, zipcode, address1, email,
		mobile, emailGet, gerdonalGet)
		values (#{name}, #{profile}, #{id}, #{birthdate}, #{pass}, #{zipcode}, #{address1}, #{email},
		#{mobile}, #{emailGet}, #{gerdonalGet})
	</insert>
	
	<select id="getMemberShip" parameterType="String">
		select * from membership where id=#{id}
	</select>
	
	<select id="memberPassCheck" resultType="String">
		select pass from membership where id=#{id}
	</select>
	
	<update id="updateMember" parameterType="MemberShip">
		update membership set
			email=#{email}, zipcode=#{zipcode}, address1=#{address1}, pass=#{pass},
			mobile=#{mobile}, emailGet=#{emailGet}, gerdonalGet=#{gerdonalGet}, profile=#{profile}
		where id=#{id}
	</update>
	
	<delete id="deleteUser" parameterType="MemberShip">
		delete from membership where id=#{id}
	</delete>
<<<<<<< HEAD
	
=======
>>>>>>> 03e8ae9b9285aa6e91b646fb24808b9094a7927e
</mapper>

